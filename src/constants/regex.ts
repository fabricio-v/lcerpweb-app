// Expressão regular para validar se não contém caracteres especiais
export const noSpecialChars = /^[a-zA-Z0-9]+$/;

// Expressão regular para validar se não contém espaços
export const noSpaces = /^\S+$/;

// Expressão regular para validar se não contém letrar, sem ser números
export const noCharsLetters = /[a-zA-Z]/;
